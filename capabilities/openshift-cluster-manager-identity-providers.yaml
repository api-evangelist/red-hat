naftiko: 1.0.0-alpha2
info:
  label: Red Hat OpenShift Cluster Manager API — Identity Providers
  description: 'Red Hat OpenShift Cluster Manager API — Identity Providers. 2 operations. Lead operation: Red Hat List Identity
    Providers. Self-contained Naftiko capability covering one Red Hat business surface.'
  tags:
  - Red Hat
  - Identity Providers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    RED_HAT_API_KEY: RED_HAT_API_KEY
capability:
  consumes:
  - type: http
    namespace: openshift-cluster-manager-identity-providers
    baseUri: https://api.openshift.com
    description: Red Hat OpenShift Cluster Manager API — Identity Providers business capability. Self-contained, no shared
      references.
    resources:
    - name: api-clusters_mgmt-v1-clusters-cluster_id-identity_providers
      path: /api/clusters_mgmt/v1/clusters/{cluster_id}/identity_providers
      operations:
      - name: listidentityproviders
        method: GET
        description: Red Hat List Identity Providers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createidentityprovider
        method: POST
        description: Red Hat Create an Identity Provider
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.RED_HAT_API_KEY}}'
  exposes:
  - type: rest
    namespace: openshift-cluster-manager-identity-providers-rest
    port: 8080
    description: REST adapter for Red Hat OpenShift Cluster Manager API — Identity Providers. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/clusters-mgmt/v1/clusters/{cluster-id}/identity-providers
      name: api-clusters-mgmt-v1-clusters-cluster-id-identity-providers
      description: REST surface for api-clusters_mgmt-v1-clusters-cluster_id-identity_providers.
      operations:
      - method: GET
        name: listidentityproviders
        description: Red Hat List Identity Providers
        call: openshift-cluster-manager-identity-providers.listidentityproviders
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createidentityprovider
        description: Red Hat Create an Identity Provider
        call: openshift-cluster-manager-identity-providers.createidentityprovider
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: openshift-cluster-manager-identity-providers-mcp
    port: 9090
    transport: http
    description: MCP adapter for Red Hat OpenShift Cluster Manager API — Identity Providers. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: red-hat-list-identity-providers
      description: Red Hat List Identity Providers
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: openshift-cluster-manager-identity-providers.listidentityproviders
      outputParameters:
      - type: object
        mapping: $.
    - name: red-hat-create-identity-provider
      description: Red Hat Create an Identity Provider
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: openshift-cluster-manager-identity-providers.createidentityprovider
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
